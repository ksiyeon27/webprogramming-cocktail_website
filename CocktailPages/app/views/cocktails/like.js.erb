import $ from 'jQuery';

$('.hearted-btn').on('ajax:success', function(){
    $(this).parent().parent().find('.like_count').html('<%= @cocktail.get_likes.size %> &#129505');$(this).closest('.hearted-btn').html('Like');
});

$('.heart-btn').on('ajax:success', function(){
    $(this).parent().parent().find('.like_count').html('<%= @cocktail.get_likes.size %> &#129505');$(this).closest('.heart-btn').html('Liked');
});